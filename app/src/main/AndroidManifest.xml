<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- 下载文件写公共Download目录: 仅API28-需要scoped storage豁免, API29+走MediaStore免权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- APK下载完成点击通知直达安装: Android 8+触发安装需"允许安装未知应用"授权 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>

    <!-- 下载完成通知点击打开文件: resolveActivity需可见能处理VIEW的包(Android 11+包可见性) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:mimeType="*/*"/>
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tool"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Tool">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".module.web.WebActivity"
            android:exported="true"
            android:theme="@style/Theme.Tool"
            android:taskAffinity="${applicationId}.web"
            android:enableOnBackInvokedCallback="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize"
            >
            <!-- 注册为浏览器: 其他应用"用浏览器打开"时可选到本应用 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
            </intent-filter>
        </activity>
        <activity android:name=".module.scan.ScanActivity"
            android:exported="false"
            android:theme="@style/Theme.Tool"
            android:taskAffinity="${applicationId}.scan"
            android:configChanges="orientation|screenSize|keyboardHidden"
            />
        <activity android:name=".module.compress.CompressActivity"
            android:exported="true"
            android:theme="@style/Theme.Tool"
            android:taskAffinity="${applicationId}.compress"
            android:label="@string/compress_label"
            >
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <category android:name="android.intent.category.OPENABLE"/>
                <data android:scheme="file"/>
                <data android:scheme="content"/>
                <data android:mimeType="*/*"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="*/*"/>
            </intent-filter>
        </activity>
        <activity android:name=".module.bluetooth.BluetoothActivity"
            android:exported="true"
            android:theme="@style/Theme.Tool"
            android:taskAffinity="${applicationId}.bluetooth">
        </activity>
        <activity android:name=".module.ip.IpActivity"
            android:exported="true"
            android:theme="@style/Theme.Tool"
            android:taskAffinity="${applicationId}.ip">
        </activity>
        <activity android:name=".module.note.NoteActivity"
            android:exported="true"
            android:theme="@style/Theme.Tool"
            android:taskAffinity="${applicationId}.note">
        </activity>
        <activity android:name=".module.reader.ReaderActivity"
            android:exported="true"
            android:theme="@style/Theme.Tool"
            android:label="阅读"
            android:screenOrientation="portrait"
            android:taskAffinity="${applicationId}.reader">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <category android:name="android.intent.category.OPENABLE"/>
                <data android:scheme="content"/>
                <data android:scheme="file"/>
                <data android:mimeType="text/plain"/>
                <data android:mimeType="application/octet-stream"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
                <data android:mimeType="application/octet-stream"/>
            </intent-filter>
        </activity>
        <provider
            android:authorities="${applicationId}.fileProvider"
            android:name="androidx.core.content.FileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"/>
        </provider>

        <!-- 下载中断通知点击续传: exported=false仅应用内显式intent可达 -->
        <receiver
            android:name="com.yukino.tool.module.web.DownloadResumeReceiver"
            android:exported="false"/>
    </application>

</manifest>